import { Appear, Embed, Notes } from "mdx-deck";
import { CodeSurferLayout } from "code-surfer";


## <a>Let`s</a> start with HTML 👨‍💻

---

<CodeSurferLayout>

```html title="Whats is HTML ?"
```

```html title="Whats is HTML ?" subtitle="HTML is markup language"
1.  HTML not a programming language
```

```html title="Whats is HTML ?"
1.  HTML not a programming language

2.  HTML is markup language
```

```html title="Whats is HTML ?"
1.  HTML not a programming language

2.  HTML is markup language

    Markup language adalah sebuah sistem penulisan
```

```html title="Whats is HTML ?"
1.  HTML not a programming language

2.  HTML is markup language

    Markup language adalah sebuah sistem penulisan
    yang memungkinkan komputer tau maksud dari sebuah content 
```

```html title="Whats is HTML ?"
1.  HTML not a programming language

2.  HTML is markup language

    Markup language adalah sebuah sistem penulisan
    yang memungkinkan komputer tau maksud dari sebuah content 

contoh:

    Judul Sebuah Cerita
```

```html title="Whats is HTML ?" subtitle="<h1></h1>memungkinkan komputer tau content adalah sebuah judul"
1.  HTML not a programming language

2.  HTML is markup language

    Markup language adalah sebuah sistem penulisan
    yang memungkinkan komputer tau maksud dari sebuah content 

contoh:

    <h1>Judul Sebuah Cerita</h1>
```
</CodeSurferLayout>

