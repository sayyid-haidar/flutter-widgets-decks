import { Appear, Embed, Notes } from "mdx-deck";
import { CodeSurferLayout } from "code-surfer";

# Bingung ? 🤔
<hr />

<Appear>
    <h3>Sebelum <a>lanjut</a> ada yang ingin di tanyakan ??</h3>
</Appear>

---

# <a>Text</a> Widget

---

<CodeSurferLayout>

```js title="Text class"
Text(
    // A run of text with a single style.
)
```

```js 1:4 title="Text class"
Text(
    // A run of text with a single style.
    'string params',
)
```

```js 1:8 title="Text class"
Text(
    // A run of text with a single style.
    'string params',
    style: TextStyle(
        fontSize: 50.0,
        color: Colors.red
    )
)
```

</CodeSurferLayout>

---

<CodeSurferLayout>

```js title="RichText class"
RichText(
    // A run of text with a single style.
)
```

```js 1:7 title="RichText class"
RichText(
    // A run of text with a single style.
    text: TextSpan(
        text: 'string text',
        style: null
    ),
)
```

```js 1:11 title="RichText class"
RichText(
    // A run of text with a single style.
    text: TextSpan(
        text: 'string text',
        style: null
    ),
    children: <TextSpan>[
        TextSpan(),
        TextSpan(),
    ]
)
```

</CodeSurferLayout>
