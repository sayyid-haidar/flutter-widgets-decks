import { Appear, Embed, Notes } from "mdx-deck";
import { CodeSurferLayout } from "code-surfer";

# Bingung ? 🤔
<hr />

<Appear>
    <h3>Sebelum <a>lanjut</a> ada yang ingin di tanyakan ??</h3>
</Appear>

---

# <a>Basic</a> Widget

---

<CodeSurferLayout>

```js title="Scaffold class"
Scaffold(
    // Implements the basic material design 
    // visual layout structure.
)
```


```js 1:8 title="Scaffold class"
Scaffold(
    // Implements the basic material design 
    // visual layout structure.
    appBar: null,
    body: null,
    drawer: null,
    bottomNavigatorBar: null,
)
```

</CodeSurferLayout>

---

<CodeSurferLayout>

```js title="AppBar class"
AppBar(
    // A material design app bar.
)
```

```js 1:8 title="Scaffold class"
AppBar(
    // A material design app bar.
    title: Text('Home'),
    leading: null,
    actions: null,
)
```

</CodeSurferLayout>

---

<CodeSurferLayout>

```js title="SafeArea class"
SafeArea(
    // SafeArea in Screen
)
```

```js 1:4 title="SafeArea class"
SafeArea(
    // SafeArea in Screen
    child: null,
)
```

```js 1:8 title="SafeArea class"
SafeArea(
    // SafeArea in Screen
    child: null,
    top: false,
    bottom: false,
    left: false,
    right: false,
)
```

</CodeSurferLayout>

---

<CodeSurferLayout>

```js title="Container class"
Container(
    // combines common painting, 
    // positioning, and sizing widgets.
)
```

```js 1:7 title="Container class"
Container(
    // combines common painting, 
    // positioning, and sizing widgets.
    height: 100.0,
    decoration: null,
    child:
)
```

</CodeSurferLayout>

---

<CodeSurferLayout>

```js title="Text class"
Text(
    // A run of text with a single style.
)
```

```js 1:4 title="Text class"
Text(
    // A run of text with a single style.
    'string params',
)
```

```js 1:8 title="Text class"
Text(
    // A run of text with a single style.
    'string params',
    style: TextStyle(
        fontSize: 50.0,
        color: Colors.red
    )
)
```

</CodeSurferLayout>

---

<CodeSurferLayout>

```js title="RichText class"
RichText(
    // A run of text with a single style.
)
```

```js 1:7 title="RichText class"
RichText(
    // A run of text with a single style.
    text: TextSpan(
        text: 'string text',
        style: null
    ),
)
```

```js 1:11 title="RichText class"
RichText(
    // A run of text with a single style.
    text: TextSpan(
        text: 'string text',
        style: null
    ),
    children: <TextSpan>[
        TextSpan(),
        TextSpan(),
    ]
)
```

</CodeSurferLayout>

---

<CodeSurferLayout>

```js title="Column class"
Column(
    // A widget that displays 
    // its children in a vertical array.
)
```

```js 1:7 title="Column class"
Column(
    // A widget that displays 
    // its children in a vertical array.
    crossAxisAlignment: CrossAxisAlignment.center,
    mainAxisAlignment: MainAxisAlignment.spaceEvenly,
    mainAxisSize: MainAxisSize.max
)
```

```js 1:11 title="Column class"
Column(
    // A widget that displays 
    // its children in a vertical array.
    crossAxisAlignment: CrossAxisAlignment.center,
    mainAxisAlignment: MainAxisAlignment.spaceEvenly,
    mainAxisSize: MainAxisSize.max.
    children: <Widget>[
        Text('text'),
        ....
    ]
)
```

</CodeSurferLayout>

---

<CodeSurferLayout>

```js title="Row class"
Row(
    // A widget that displays 
    // its children in a horizontal array.
)
```

```js 1:7 title="Row class"
Row(
    // A widget that displays 
    // its children in a horizontal array.
    crossAxisAlignment: CrossAxisAlignment.center,
    mainAxisAlignment: MainAxisAlignment.spaceEvenly,
    mainAxisSize: MainAxisSize.max
)
```

```js 1:11 title="Row class"
Row(
    // A widget that displays 
    // its children in a horizontal array.
    crossAxisAlignment: CrossAxisAlignment.center,
    mainAxisAlignment: MainAxisAlignment.spaceEvenly,
    mainAxisSize: MainAxisSize.max.
    children: <Widget>[
        Text('text'),
        ....
    ]
)
```

</CodeSurferLayout>

---

<CodeSurferLayout>

```js title="RaisedButton class"
RaisedButton(
    // A material design "raised button".
)
```

```js 1:6 title="RaisedButton class"
RaisedButton(
    // A material design "raised button".
    onPressed: () {
        // action
    },
)
```

```js 1:8 title="RaisedButton class"
RaisedButton(
    // A material design "raised button".
    onPressed: () {
        // action
    },
    child: null,
    color: Colors.red
)
```

</CodeSurferLayout>