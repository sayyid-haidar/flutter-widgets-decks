import { Appear, Embed, Notes } from "mdx-deck";
import { CodeSurferLayout } from "code-surfer";

# Bingung ? 🤔
<hr />

<Appear>
    <h3>Sebelum <a>lanjut</a> ada yang ingin di tanyakan ??</h3>
</Appear>

---

# <a>asset, image, icon</a> Widget

---

<CodeSurferLayout>

```markup title="Asset Bundle 💻"
```

```markup title="Asset Bundle 💻"
├─ /android
├─ /build
├─ /ios
├─ /lib
├─ /test
└─ pubspec.yaml
```


```markup  title="Asset Bundle 💻"
├─ /android
├─ /assets
├─ /build
├─ /ios
├─ /lib
├─ /test
└─ pubspec.yaml
```


```markup 1:15  title="Asset Bundle 💻"
└─ pubspec.yaml

flutter:

  # The following line ensures that the Material Icons font is
  # included with your application, so that you can use the icons in
  # the material Icons class.
  uses-material-design: true

  # To add assets to your application, add an assets section, like this:
  assets:
    - assets/about.json
    - assets/rules.json
    - assets/tips.json
    - assets/notification.png
```

</CodeSurferLayout>

---

<CodeSurferLayout>

```js title="Image Class"
Image(
    // Retrives image from an ImageProvider class
)

Image.network(
    // Retrives image from a URL path
)
```


```js 1:10 title="Image Class"
Image(
    // Retrives image from an ImageProvider class
    image: AssetImage('assets/images/logo.png'),
    fit: BoxFit.cover
)

Image.network(
    // Retrives image from a URL path
    'https://images-online.com'
)
```

```js 1:10 title="Icon Class"
Icon(
    Icons.brush,
    color: Colors.red,
    size: 10.0
)
```



</CodeSurferLayout>